Follow `AGENTS.md` for repository engineering rules and `spec.md` for product behaviour and requirements.

Inspect the actual code and configuration before making assumptions. Before shipping, run the repository validation commands: `npm run lint`, `npm run test:coverage`, `npm run build`, and `npm run audit:type`.
