import { GrowthFormulaRail } from "ajmal-diagnostic";

/** The six-step growth formula rail: Vision, Strategy, Systems, People, Execution, Accountability. */
export function Default() {
  return <GrowthFormulaRail />;
}
