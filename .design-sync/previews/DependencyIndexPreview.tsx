import { DependencyIndexPreview } from "ajmal-diagnostic";

/** The Business Health Check teaser — what the check measures, and the CTA into it. */
export function Default() {
  return <DependencyIndexPreview />;
}
