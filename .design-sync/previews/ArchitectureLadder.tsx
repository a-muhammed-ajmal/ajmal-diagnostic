import { ArchitectureLadder } from "ajmal-diagnostic";

/**
 * Ascending bars for the six operating-architecture layers, Founder through
 * Scale. The top rung is the amber fill — the one place amber carries area.
 */
export function Default() {
  return <ArchitectureLadder />;
}
