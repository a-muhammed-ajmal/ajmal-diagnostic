import { CommercialLadder } from "ajmal-diagnostic";

/** The five-stage commercial ladder, from Health Check through Growth Partner Retainer. */
export function Default() {
  return <CommercialLadder />;
}
