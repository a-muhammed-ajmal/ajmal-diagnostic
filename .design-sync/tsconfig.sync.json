{
  "_comment": "Read ONLY by design-sync's tsconfigPathsPlugin for compilerOptions.paths. Paths resolve relative to this file's directory. Aliasing the two next imports keeps Next router internals out of the DS bundle. NOTE: do not use a key named with two slashes here - the plugin's comment stripper truncates the JSON and silently disables all path mapping.",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["../src/*"],
      "next/link": ["./shims/next-link.tsx"],
      "next/navigation": ["./shims/next-navigation.ts"]
    }
  }
}
