import { FdiDiagnosticFlow } from '@/components/fdi/FdiDiagnosticFlow';

/** The Business Health Check is the only public diagnostic. */
export default function DiagnosticPage() {
  return <FdiDiagnosticFlow />;
}
