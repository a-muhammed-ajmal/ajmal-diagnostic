import { FdiResults } from '@/components/fdi/FdiResults';

/** The canonical, browser-private result page for a completed Business Health Check. */
export default function ResultsPage() {
  return <FdiResults />;
}
